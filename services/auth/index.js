const registerUser = require("./registerUser");

const checkUser = require("./checkUser");

const setTokenLogin = require("./setTokenLogin");

const logoutUser = require("./logoutUser");

module.exports = {
  registerUser,
  checkUser,
  setTokenLogin,
  logoutUser,
};
